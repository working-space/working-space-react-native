import styled from '@emotion/native';

import { GrayColor } from 'src/utils/color';

export const FavoritesStyled = styled.SafeAreaView`
  flex: 1;
  background-color: ${GrayColor.GRAY_0};
`;

export const FavoritesView = styled.View`
  flex: 1;
  padding: 16px;
`;
